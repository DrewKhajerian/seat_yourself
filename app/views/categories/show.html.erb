<h1><%= @category.name %></h1>

<% @category.restaurants.each do |rest| %>
	<p><%= link_to rest.name, restaurant_path(rest) %></p>
	<p><%= rest.address %></p>
	<p><%= rest.description %></p>

<% end %>